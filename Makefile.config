# -----------------------------------------------------------------------
#
#   Configuration Makefile for RH
#
#   Edit the variables below to reflect your compilers and environment.
#
# -----------------------------------------------------------------------

OPT = -O3 -qopt-prefetch -use-intel-optimized-headers -march=core-avx2 -fp-model source
#OPT = -O2 -g -traceback -ip -qopt-prefetch -use-intel-optimized-headers -march=core-avx2 -fp-model source

CC       = mpiicc
CFLAGS   = ${OPT} -DHAVE_F90 -Wformat

F90C     = mpiifort
F90FLAGS = ${OPT}

LD       = mpiicc
LDFLAGS  =

AR       = ar
ARFLAGS  = rs
RANLIB   = ranlib

HDF5_DIR = /astro/local/hdf5/1.10.1/intel/

# The OS variable should have the operative system name
# If the command below is unavailable, please edit manually
OS = $(shell uname -s)
